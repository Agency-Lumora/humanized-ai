export const metadata = { title: "Partner with Us | Lumora Agency", description: "Collaborate with Lumora for project delivery and client referrals." };

export default function PartnershipLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
