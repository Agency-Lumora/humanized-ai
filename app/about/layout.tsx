export const metadata = { title: "About | Lumora Agency", description: "Learn about Lumora and how we partner with ambitious businesses." };

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
